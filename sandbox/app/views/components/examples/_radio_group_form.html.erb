<%= form_with url: "#", scope: :subscription do |form| %>
  <%= ui_radio_group(aria: { label: "Billing cycle" }) do %>
    <div class="flex items-center gap-2">
      <%= ui_radio_group_item(
            name: form.field_name(:cycle),
            value: "monthly",
            id: form.field_id(:cycle, "monthly"),
            checked: true
          ) %>
      <%= ui_label(for: form.field_id(:cycle, "monthly")) { "Monthly" } %>
    </div>
    <div class="flex items-center gap-2">
      <%= ui_radio_group_item(
            name: form.field_name(:cycle),
            value: "yearly",
            id: form.field_id(:cycle, "yearly")
          ) %>
      <%= ui_label(for: form.field_id(:cycle, "yearly")) { "Yearly" } %>
    </div>
  <% end %>
<% end %>
