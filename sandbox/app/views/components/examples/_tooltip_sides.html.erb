<% { top: "Top", right: "Right", bottom: "Bottom", left: "Left" }.each do |side, label| %>
  <%= ui_tooltip do %>
    <%= ui_tooltip_trigger(variant: :outline) { label } %>
    <%= ui_tooltip_content(side: side) { "On the #{side}" } %>
  <% end %>
<% end %>
